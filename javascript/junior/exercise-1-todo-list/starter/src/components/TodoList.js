import React from 'react';
import TodoItem from './TodoItem';

// This component should render the list of todo items
function TodoList({ todos, toggleTodo, deleteTodo }) {
  // TODO: Implement rendering a list of TodoItem components
  
  return (
    <div className="todo-list">
      {/* TODO: Map through todos and render a TodoItem for each one */}
    </div>
  );
}

export default TodoList;
